## How to replicate the analysis of CFCNN tolerance to optical aberrations

* Run `aberrations_vectors.ipynb` to generate synthetic data for further analysis.
* Use `process_cnn.py`.
* TODO: clean this directory. See `aberrated-star` directory, where we do both single point AND star.